import image from './image.png';
const WealthEvolution = () => {
    return (
        <div>
            Verificar necessidade de uma tela pra isso. Ou se pode ser exibido junto em outra tela.
            <img src={image} />
        </div>
    );
};

export default WealthEvolution;
